{
	"name": "better-commits",
	"displayName": "Better Commits",
	"description": "VSCode extension that keeps your commit messages consistent",
	"version": "1.6.0",
	"publisher": "ugi-dev",
	"engines": {
		"vscode": "^1.80.0"
	},
	"license": "MIT",
	"homepage": "https://ugi.dev",
	"bugs": {
		"url": "https://github.com/ugi-dev/better-commits/issues"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/ugi-dev/better-commits"
	},
	"icon": "icon.png",
	"keywords": [
		"git",
		"commits",
		"vscode",
		"better commits",
		"ugi-dev"
	],
	"categories": [
		"Other"
	],
	"main": "./out/index.js",
	"activationEvents": [],
	"contributes": {
		"commands": [
			{
				"command": "extension.betterCommits",
				"title": "Better Commits",
				"category": "Other",
				"icon": "icon.png"
			}
		],
		"menus": {
			"scm/title": [
				{
					"command": "extension.betterCommits",
					"group": "navigation",
					"when": "scmProvider == git"
				}
			]
		},
		"keybindings": [
			{
				"command": "extension.betterCommits",
				"key": "ctrl+alt+enter",
				"mac": "cmd+shift+enter"
			}
		],
		"configuration": {
			"title": "Better Commits",
			"properties": {
				"betterCommits.useDefaultTypes": {
					"type": "boolean",
					"default": true,
					"description": "Use built-in commit types."
				},
				"betterCommits.types": {
					"type": "array",
					"default": [],
					"description": "Custom commit types. If useDefaultTypes is false, only these are shown.",
					"items": {
						"type": "object",
						"required": [
							"label",
							"description"
						],
						"properties": {
							"label": {
								"type": "string",
								"description": "Commit type label, e.g. feat or fix."
							},
							"description": {
								"type": "string",
								"description": "Description shown in the picker."
							}
						}
					}
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile && npm run lint",
		"lint": "npx @biomejs/biome format --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@types/glob": "^8.1.0",
		"@types/mocha": "^10.0.1",
		"@types/node": "16.x",
		"@types/vscode": "^1.80.0",
		"@vscode/test-electron": "^2.2.3",
		"glob": "^8.1.0",
		"mocha": "^10.2.0",
		"typescript": "^4.9.5"
	}
}
